CompanyTrustIndex has been deployed.

To get the service endpoint:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
  kubectl port-forward service/{{ include "company-trust-index.fullname" . }} 8080:{{ .Values.service.port }}
  Then open http://127.0.0.1:8080
{{- end }}

Set `secret.appSecret` and `secret.databaseUrl`, or use `existingSecret`, before deploying to a real environment.
